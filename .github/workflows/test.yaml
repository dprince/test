name: Force Push Branch

on:
  push:
    branches:
      - '*'

jobs:
  call-build-workflow:
    uses: dprince/openstack-k8s-operators-ci/.github/workflows/release-branch-sync.yaml@release-branch-sync
